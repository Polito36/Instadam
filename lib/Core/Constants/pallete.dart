import 'package:flutter/material.dart';

class Pallete {
  static const bgColor = Colors.white;
  static const primaryTextColor = Color(0xFF010A1C);
  static const buttonColor = Color(0xFF125BE4);
  static const secondaryTextColor = Color(0xFFB1BCD0);
}
